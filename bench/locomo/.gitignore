# Third-party dataset. locomo10.json is not ours to redistribute.
# See DATA.md for the download source and the expected sha256.
data/

# Corpus, rankings, mem0 dumps, logs. Regenerated by mode_a.sh; large.
work/

# Output of YOUR runs. The committed evidence behind the published tables lives in
# results/ and is never written to by the harness -- that separation is the point.
out/

# Local model cache for the mem0 embedder and the ONNX sentence encoder.
hf-cache/

node_modules/
.venv/
__pycache__/
*.pyc
